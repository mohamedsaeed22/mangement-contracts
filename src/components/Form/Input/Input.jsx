import { Form } from "react-bootstrap";

const Input = ({
  label,
  name,
  type = "text",
  register,
  error,
  formText,
  success,
  disabled,
}) => {
  return <input />;
};

export default Input;
